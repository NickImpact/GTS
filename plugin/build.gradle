jar {
    archiveName = "GTS-${project.ext.sponge}-${project.version}.jar"
}

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: '../libs', include: '*.jar')
    compile group: 'com.zaxxer', name: 'HikariCP', version: '2.5.1'
}
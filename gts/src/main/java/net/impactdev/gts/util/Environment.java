package net.impactdev.gts.util;

// TODO - On top of established identifiers, we need to also provide a means for environment detail lookup.
// ^ For example, the Reforged extension would need a hard dependency on forge and pixelmon, so the
//   extension should be able to ask the environment prior to its construction if both are available
public final class Environment {
}
